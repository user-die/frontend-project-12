{
  "name": "@hexlet/code",
  "version": "1.0.0",
  "description": "Project Chat",
  "main": "index.js",
  "scripts": {
    "build": "npm run build --prefix frontend",
    "start": "npx start-server -s ./frontend/build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd frontend && npm ci"
  },
  "author": "Konstantin Kolmakov",
  "license": "ISC",
  "dependencies": {
    "@hexlet/chat-server": "^2.0.2"
  },
  "devDependencies": {
    "@eslint/compat": "^1.0.3",
    "@eslint/js": "^9.4.0",
    "eslint": "^9.4.0",
    "eslint-plugin-react": "^7.34.2",
    "globals": "^15.3.0"
  }
}
